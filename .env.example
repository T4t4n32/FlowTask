# Copia este archivo como .env y rellena los valores. El .env real NO se sube a git.

# --- Telegram ---
# Token que te da @BotFather al crear el bot.
TELEGRAM_TOKEN=
# 1 = el bot pregunta a Telegram por mensajes (máquina propia, sin URL pública). Recomendado.
# 0 = modo webhook (necesita URL pública).
TELEGRAM_POLLING=1

# --- IA (Google Gemini) ---
# Clave de https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Modelo (opcional). Barato: gemini-flash-lite-latest | Más potente: gemini-flash-latest
GEMINI_MODEL=gemini-flash-lite-latest

# --- Base de datos ---
# Local (por defecto si se deja vacío): sqlite:///./flowtask.db
# Supabase (Task 3): cadena del "Transaction pooler", puerto 6543, de
#   Supabase > Project Settings > Database > Connection string > Transaction pooler
DATABASE_URL=
